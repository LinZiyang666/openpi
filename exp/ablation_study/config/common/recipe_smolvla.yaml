# SmolVLA distillation finetune recipe (Phase 1). Frozen inputs for
# train_smolvla.py; checkpoint selection uses the student-val slice only.
base_checkpoint: lerobot/smolvla_base
steps: 20000
batch_size: 64
extra_args: []

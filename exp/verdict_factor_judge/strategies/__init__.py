"""Experiment strategies (policy layer) for the conductor framework.

These plug into ``openpi.conductor`` via the ``ExperimentStrategy`` interface.
Mechanism lives in ``src/openpi/conductor``; experiment semantics live here.
"""

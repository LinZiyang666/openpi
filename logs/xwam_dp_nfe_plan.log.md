# X-WAM 与 Diffusion Policy DDPM-100 减步阶梯 — 立项与环境记录（2026-09-16 深夜起）

> owner 23:5x 裁定：latent-MPC 四路调研（`logs/cache_transfer/latent_mpc_summary.md`）后，把 **X-WAM × RoboCasa-2024** 与 **Diffusion Policy DDPM-100 × robomimic** 的初步减步实验排进队列（顺序：Cosmos RC24 → X-WAM → DDPM-100），环境先配。数据摆放纪律：wls 代码+venv 在 SSD `/home/weiland/x-wam`、模型/缓存/结果在 `/data/xwam`；h100 全在 `/data/xwam`（根盘只剩 8 GB）；timan108 在 `/scratch/zixuans8/{xwam,dp}`（t107 /scratch 只剩 6.5 GB，不放新环境）。

## 1. X-WAM（sharinka0715/X-WAM，Apache-2.0；Wan2.2-TI2V-5B 底座，多视角视频 + 动作联合去噪，ANS：动作 T_a 步早停、视频 T_O 步）
- 评测架构自带 broker（ZMQ）+ policy_server + robocasa_client：server `--denoise_steps <视频步> --action_denoise_steps <动作步>`，client 24 任务、obj split B、layout ((1,1),(2,2),(4,4),(6,9),(7,10))、3 路 256² 相机、每次查询 32 步开环、`--num_evals_per_worker`/`--world_size` 切分。**减步 ladder = 只改 server 的 `--action_denoise_steps`**（10/5/3/2/1），视频步数固定。
- 论文口径：RoboCasa 24 任务 79.2%；3090 上 1.03 s/动作（动作早停）/4.7 s（全跑）。
- 环境脚本 `exp/xwam_nfe/ops/setup_xwam.sh <code> <data> <server|client> <uv home> [uv cache]`：torch 2.8.0+cu129、requirements.txt、flash-attn 2.8.3 预编译 wheel（cxx11abiTRUE）；server 另下 `sharinka0715/X-WAM-checkpoints`（含 robocasa_sft）与 `Wan-AI/Wan2.2-TI2V-5B` 到 `<data>/{checkpoints,wan22_5b}`；client 只装 robosuite/robocasa 子模块（RoboCasa-2024，robosuite 1.5.2）+ kitchen assets + numpy 1.23.5。
- 23:49 起装：wls `/home/weiland/x-wam`（tmux `xwam_setup`，日志 `/tmp/xwam/setup.log`）、h100 `/data/xwam/X-WAM`、t108 `/scratch/zixuans8/xwam/X-WAM`（client import 已 ok，assets 下载中）。

## 2. Diffusion Policy DDPM-100（real-stanford/diffusion_policy 官方；robomimic image 任务 + PushT）
- 官方 ckpt：`https://diffusion-policy.cs.columbia.edu/data/experiments/image/<task>/diffusion_policy_cnn/train_0/checkpoints/epoch=*.ckpt`（每个 ~4 GB，含 EMA+optimizer），取 square_mh / can_mh / transport_mh / tool_hang_ph / pusht → `/scratch/zixuans8/dp/ckpts/<task>/`（tmux `dp_ckpts`）。
- 环境：官方 `conda_environment.yaml`（py3.9、torch 1.12.1 cu116、free-mujoco-py 2.1.6、cheng-chi robosuite fork、robomimic 0.2.0、diffusers 0.11.1）→ timan108 无 sudo/无 GL 头文件，用 **micromamba** 建到 `/scratch/zixuans8/dp/env`，yaml 加 conda-forge 的 glew/mesalib/glfw/patchelf/libglu/gcc11 顶替 apt 包；MuJoCo 2.1.0 二进制在 `/scratch/zixuans8/dp/mujoco210`（`MUJOCO_PY_MUJOCO_PATH`）。脚本 `exp/dp_nfe/ops/setup_dp_t108.sh`（tmux `dp_setup`，日志 `/tmp/dp/setup.log`）。坑：micromamba tar 用 `--strip-components=1` 解到了 `$D/` 而不是 `$D/bin/`（已修）。
- 减步 ladder 设计：同一 ckpt，hydra 覆盖 `policy.num_inference_steps` 与 scheduler（DDPM 100/50/20/10 与 DDIM 10/4/2/1），每任务 50 集，记每次决策延迟；文献悬崖：DDIM-1 全 0、DDIM-2 均值 0.64 / PushT 0.29（满步 0.94）。

## 3. 进度（2026-09-17 00:05）
- X-WAM 环境：h100 server 装完（torch 2.8.0+cu129、flash-attn 2.8.3、ckpt `robocasa_sft`/`pretrained`/`robotwin_sft` + Wan2.2-TI2V-5B 都在 `/data/xwam`）；wls server ckpt 还在下（HDD）；t108 client 装完（assets 到位）。DP：micromamba env 求解中，5 个 ckpt 已下 4 个。
- **X-WAM 冒烟第一次失败（00:00）**：h100 暂停 2 个 Cosmos replica 腾出 26 GB 不够——`XWAMRunner(config).cuda().bfloat16()` 先以 fp32 上卡再转 bf16，5B DiT + UMT5-XXL 峰值 >45 GB，OOM。已恢复 10 个 replica。owner 的顺序是 Cosmos → X-WAM，所以冒烟改为自动链：t108 的 RC24 ladder 打出 `RC24 FINISHED` → `xwam_arm`（t108）向 h100 kdone 监听器发 `DONE k=99` → `xwam_arm`（h100）停 10 个 Cosmos replica → 起 broker :23270/:23271 + policy_server（action 10 步、视频 50 步，`torch.compile` 首调用要几分钟）→ t108 探到 :23270 后 4 min 起冒烟 client（任务 rank 23 × 2 集，GPU 2），结果 `/scratch/zixuans8/xwam/results_smoke`、日志 `/tmp/xwam/xwam_smoke_r23.log`。server 端每次查询打印 `Inferred in X s`。
- 阶梯方案：X-WAM 的 `early_stop=True` 本身就是"动作在第 T_a 步解码、视频不续跑"的纯策略模式，ladder 只改 `--action_denoise_steps`（10/5/3/2/1）；client 按 24 任务 rank 切分，`--num_evals_per_worker 50`。
- **00:40 timan108 CUDA 不可用**：`cuInit` 直接返回 101（invalid device ordinal），任何 CUDA_VISIBLE_DEVICES 组合都一样；驱动看到 4 张 A5000（含 0000:c1:00.0），`nvidia-smi` 只列 3 张——第 4 张掉了把整机 cuInit 拖死（需要 root 重置/重启）。EGL 渲染不受影响（这也是今天所有 client 都能跑的原因；Cosmos 分片日志里的 "CUDA initialization: Unexpected error" 警告就是它）。DP 环境在 t108 上其余都通了（mujoco-py 编译要 `libGL.so`/`libEGL.so` 符号链接进 env/lib、patchelf 进 PATH、`huggingface_hub==0.20.3` 配 diffusers 0.11.1；dm-control 去掉），但模型上不了卡。
- **改在 weilandserver 跑 DP**（记录为对"server 机不跑模拟器"规则的一次偏离：t108 CUDA 坏、t107 /scratch 只剩 6.5 GB 装不下 30 GB；DP 是单进程策略+轻量 robosuite 仿真，4090 上只占 2–3 GB，与后面的 X-WAM server 共存无碍）。摆放：代码+env `/home/weiland/dp`（SSD），ckpt/数据 `/data/dp`。脚本 `setup_dp_wls.sh`/`download_dp_ckpts_wls.sh`/`download_dp_data_wls.sh`（tmux 同名，日志 `/tmp/dp/*.log`）。

## 4. 进度（2026-09-17 01:20）
- **X-WAM 阶梯在跑**（本地 tmux `xwam_ladder` 控制器 `exp/xwam_nfe/ops/ladder_xwam_local.sh 10,5,3,2,1 50 50`，日志 `$CLAUDE_JOB_DIR/tmp/xwam_ladder.log`）：k=10 从 00:27 起，h100 两个 server（0.76 s/查询，GPU 100%/600 W），01:07 wls server（0.95 s/查询）也挂进 h100 broker；t108 24 个 client 各一任务 × 50 集，结果 `/scratch/zixuans8/xwam/results_k<k>/eval_results_<rank>.json`，每档结束 merge + 拉回 `exp/xwam_nfe/data/results_k<k>/`。冒烟：TurnOffMicrowave 2/2，H100 空载 0.39 s/查询（动作 10 步）。server 改动：`policy_server.py` 先 `.bfloat16()` 再 `.cuda()`、ckpt `map_location="cpu"`（否则 fp32 上卡峰值 >45 GB）。
- **DP 阶梯在跑**（wls tmux `dp_ladder`，`exp/dp_nfe/ops/ladder_dp_wls.sh 50 25`，日志 `/tmp/dp/ladder.log`，结果 `/data/dp/results/<task>/<sched><steps>/summary.json`）：任务 square_mh/can_mh/tool_hang_ph/transport_mh/pusht × ddpm100/ddim10/ddim4/ddim2/ddim1/ddpm10，每格 50 集、25 并行 env。冒烟 square_mh ddpm100 2 集 1/2，batch-1 延迟 1507 ms（4090 与 X-WAM server 共卡，偏高——延迟要在卡空时单独重测）。坑：lowdim hdf5 的 env_kwargs 没有相机 → 按 ckpt 的 rgb 观测键注入 camera_names/84×84（`eval_dp_steps.py`）；ladder 首轮全 FAIL 是结果目录没 mkdir。
- **02:10 DP square_mh 阶梯完整**（50 集/格，4090 与 X-WAM server 共卡，延迟偏高）：DDPM-100 **0.78**（2403 ms）、DDIM-10 **0.86**（262 ms）、DDIM-4 **0.60**（124 ms）、DDIM-2 **0.00**（38 ms）、DDIM-1 **0.00**（24 ms）、DDPM-10 **0.00**（142 ms）。ε-prediction DDPM 头的悬崖如文献：DDIM 到 4 步开始掉、2 步归零、DDPM 直接砍到 10 步归零——与 Cosmos（x₀/EDM 头，1 步无损）正相反，这就是 warm 档的 counterfactual。can_mh 起跑。
- **02:25 X-WAM k=10 23/24 任务出数**：macro **0.798**（918/1150；官方 79.2%，复现对上）。最低 TurnOffStove 0.40、CoffeeSetupMug 0.46、PnPCounterToMicrowave 0.48；最高 CloseDrawer 1.00、CloseSingleDoor 0.98。剩 rank 0（PnPCounterToCab）：其 client 起跑即卡在 env 初始化 2 h（日志只有 import 警告，CPU 0.5%），杀掉重投。**X-WAM 评测两坑**：① client 写完结果后卡在退出（zmq linger），tmux 会话不消失；② 一任务一 client 串行 50 集 → 每档长尾 20–30 min。控制器换 v2：每任务拆 2 个 client（rank r 与 r+24 同任务、seed 段不重叠，`merge_results` 按任务合并）、池上限 24、回收已出结果的会话；k=10 先用 SPLIT=1 补完 rank 0，再跑 5,3,2,1。
- DP can_mh：DDPM-100 **1.00**（50/50）。
- **02:40 X-WAM 第三个坑（根因）**：broker 保留已死 server 的 READY 身份，server 重启后第一个撞上陈旧身份的 client 请求永远得不到回复（client 在 zmq poll 里睡死、CPU 0）。rank 0 两次"卡在 env 初始化"其实都是这个。修法：**server 重启必须连 broker 一起重启**（server 连上时才发 READY），控制器 v2 已改；本次手动重启 broker+3 server 后由控制器自动重投 rank 0。
- **03:00 DP can_mh 阶梯完整**：DDPM-100 **1.00** / DDIM-10 0.98 / DDIM-4 1.00 / DDIM-2 0.76 / DDIM-1 0.00 / DDPM-10 0.00。简单任务的悬崖后移到 2 步，但 DDPM 直接砍步和 1 步仍归零。tool_hang_ph 起跑。X-WAM k=10 rank 0 补跑 37/50。
- **03:08 X-WAM k=10（动作 10 步，官方默认）全 24 任务 × 50 集完成**：macro **0.7917**（950/1200），官方 79.2% **精确复现**。已拉回 `exp/xwam_nfe/data/results_k10/`（eval_results_*.json + eval_summary.json）。控制器 v2 接着跑 k=5,3,2,1（每任务 2 client、broker 随 server 重启）。
- 03:19 控制器 v2a 在 k=10 DONE 后崩（我在它运行中改了脚本文件 → bash 读偏移错位，教训：改控制器先复制成冻结副本再起）；k=5,3,2,1 从冻结副本重起，k=5 UP 03:19。DP tool_hang_ph DDPM-100 **0.82**（1521 ms）。
- **04:05 DP tool_hang_ph 阶梯完整**：DDPM-100 **0.82** / DDIM-10 0.88 / DDIM-4 0.78 / DDIM-2 0.74 / DDIM-1 0.00 / DDPM-10 0.00。transport_mh 起。X-WAM k=5 24/48 分片完。
- **04:26 X-WAM k=5 完成**（24 任务 × 50 集，2 client/任务 + broker 随重启，67 min 含尾巴）：macro **0.7808**（937/1200）vs k=10 0.7917（−1.1 pp，噪声内）。h100 server 0.43 s/查询（k=5，含排队）。k=3 起。
- **05:31 X-WAM k=3 完成**：macro **0.7858**（943/1200）；h100 0.32 s/查询。k=10/5/3 = 0.792/0.781/0.786——到 3 步仍无损。k=2 起。
- **06:39 X-WAM k=2 完成**：macro **0.7683**（922/1200）；h100 0.27 s/查询。k=10/5/3/2 = 0.792/0.781/0.786/0.768（2 步 −2.3 pp，噪声边缘）。k=1 起（最后一档）。
- **06:48 DP transport_mh 阶梯完整**：DDPM-100 **0.82** / DDIM-10 0.84 / DDIM-4 0.84 / DDIM-2 0.80 / DDIM-1 0.00 / DDPM-10 0.00。pusht 起（最后一个任务）。
- 06:51 DP pusht 6 档全 FAIL（3 min 内）：`eval_dp_steps.py` 的相机注入按"键以 `_image` 结尾或 type=rgb"取相机名，PushT 唯一的 rgb 键就叫 `image` → 相机名截成空串 → `KeyError '_image'`。修：只对以 `_image` 结尾的键注入（robomimic 任务），PushT（gym env，无相机 kwargs）不注入；已推 wls，ladder 重起（已有 summary 的 24 格自动跳过），pusht ddpm100 06:52 UP。
- **07:02 DP 阶梯全部完成**（5 任务 × 6 档 × 50 集；summary 已拉回 `exp/dp_nfe/data/results/<task>/<cell>/summary.json`，汇总 `exp/dp_nfe/data/aggregate_dp.json`，脚本 `exp/dp_nfe/aggregate_dp.py`）。pusht：DDPM-100 **0.85** / DDIM-10 0.85 / DDIM-4 0.82 / DDIM-2 0.72 / DDIM-1 0.14 / DDPM-10 0.14（PushT 分数=覆盖率非二值，ckpt 名义 0.884 是官方 22 seed 的最优 epoch）。

  | task | DDPM-100 | DDIM-10 | DDIM-4 | DDIM-2 | DDIM-1 | DDPM-10 |
  |---|---|---|---|---|---|---|
  | square_mh | 0.78 | 0.86 | 0.60 | 0.00 | 0.00 | 0.00 |
  | can_mh | 1.00 | 0.98 | 1.00 | 0.76 | 0.00 | 0.00 |
  | tool_hang_ph | 0.82 | 0.88 | 0.78 | 0.74 | 0.00 | 0.00 |
  | transport_mh | 0.82 | 0.84 | 0.84 | 0.80 | 0.00 | 0.00 |
  | pusht | 0.85 | 0.85 | 0.82 | 0.72 | 0.14 | 0.14 |
  | **mean** | **0.85** | **0.88** | **0.81** | **0.60** | **0.03** | **0.03** |

  结论：ε-prediction DDPM-100 头减步有硬悬崖——DDIM 到 4 步 −4 pp、2 步 −25 pp、1 步归零；DDPM 采样器直接砍到 10 步同样归零（DDPM 每步加噪、少步时 ε 估计累积偏差）。与 π0.5/GR00T/Cosmos/X-WAM（flow / x₀ 头，1–2 步近无损）形成对照：warm 档"少步续算"对 DP 这类 ε-DDPM 头不可用，须走 DDIM 且 ≥4 步。延迟列（batch-1，4090 与 X-WAM server 共卡时测）待 X-WAM 停后用 `ops/latency_dp_wls.sh`（`eval_dp_steps.py --latency_only`）在空卡重测。
- **07:42 X-WAM k=1 完成 → 阶梯全部完成**：k=1 macro **0.7933**（952/1200）；h100 server 0.22 s/查询。控制器已停 h100 两 server + wls server，我另停了 h100 broker 与 t108 残留的冒烟 client；三机 GPU 归零。结果 `exp/xwam_nfe/data/results_k{10,5,3,2,1}/`，汇总 `exp/xwam_nfe/data/aggregate_xwam.json`。

  | k（action_denoise_steps） | 10（官方默认） | 5 | 3 | 2 | 1 |
  |---|---|---|---|---|---|
  | macro SR（24 任务 × 50 集） | **0.7917** | 0.7808 | 0.7858 | 0.7683 | **0.7933** |
  | h100 server s/查询（含排队，2 server 同卡） | 0.76 | 0.43 | 0.32 | 0.27 | 0.22 |

  逐任务（k=10/5/3/2/1）：CloseDoubleDoor .80/.78/.86/.88/.84、CloseDrawer 1.0/.98/1.0/1.0/.98、CloseSingleDoor .98/.96/.96/.96/.98、CoffeePressButton .96/.98/.98/.98/1.0、CoffeeServeMug .84/.90/.90/.86/.94、CoffeeSetupMug .46/.44/.54/.44/.54、OpenDoubleDoor .96/.92/.92/.90/.90、OpenDrawer .88/.92/.92/.94/.90、OpenSingleDoor .66/.86/.76/.80/.92、PnPCabToCounter .78/.70/.66/.64/.62、PnPCounterToCab .64/.56/.60/.58/.64、PnPCounterToMicrowave .48/.36/.50/.32/.42、PnPCounterToSink .80/.84/.82/.82/.86、PnPCounterToStove .84/.84/.82/.80/.84、PnPMicrowaveToCounter .60/.48/.40/.46/.50、PnPSinkToCounter .72/.72/.74/.72/.72、PnPStoveToCounter .88/.72/.70/.68/.64、TurnOffMicrowave .96/.98/.98/.96/.96、TurnOffSinkFaucet .82/.78/.78/.78/.78、TurnOffStove .40/.42/.42/.46/.40、TurnOnMicrowave .96/.88/.90/.86/.94、TurnOnSinkFaucet .88/.96/.90/.86/.94、TurnOnStove .82/.84/.90/.86/.88、TurnSinkSpout .88/.92/.90/.88/.90。

  结论：X-WAM 的动作头（Wan 流匹配/x₀ 型联合去噪，动作在第 T_a 步早停解码）**1 步无损**（0.793 vs 0.792，k=2 的 0.768 是 50 集/任务的噪声：单任务 ±0.07 波动）；与 Cosmos（1 步无损）、π0.5/GR00T（LIBERO/RoboCasa365 近无损）同族，只有 DP 的 ε-DDPM 头有悬崖。PnP 类任务（PnPStoveToCounter .88→.64、PnPCabToCounter .78→.62）随 k 下降有单调迹象，但每格只 50 集，需要更多集才能判定——作为初步减步实验足够。
- 07:42 DP 延迟重测起（wls tmux `dp_latency`，`/tmp/dp/latency_dp_wls.sh`，4090 空卡 `gpu_procs=0`，每格写 `latency.json`，日志 `/tmp/dp/latency.log`）。
- **07:53 DP batch-1 延迟空卡重测完成**（4090 无其它进程，`latency.json` 已拉回并并入 `aggregate_dp.json` 的 `batch1_latency_ms_idle_gpu`）。共卡期的数字偏高约 1.5–1.8×，正式引用用下表：

  | task | DDPM-100 | DDIM-10 | DDIM-4 | DDIM-2 | DDIM-1 | DDPM-10 |
  |---|---|---|---|---|---|---|
  | square_mh | 1305 ms | 144 | 64 | 37 | 24 | 144 |
  | can_mh | 1331 | 142 | 64 | 39 | 25 | 138 |
  | tool_hang_ph | 1339 | 145 | 65 | 38 | 26 | 146 |
  | transport_mh | 1363 | 155 | 75 | 48 | 34 | 153 |
  | pusht | 1333 | 139 | 60 | 32 | 19 | 138 |

  ≈ 13 ms/去噪步 + 约 11 ms 固定开销（视觉编码 + Python 循环），launch-bound 的 CNN U-Net；DDPM-100 的 1.3 s 是这一族在 4090 上的真实决策成本，DDIM-10 便是 9× 提速、DDIM-4 20×（成功率 −4 pp）。wls `dp_latency` tmux 已退出、GPU 0 MiB。

## 5. 收工状态（2026-09-17 07:55）
- 四机 GPU 全归零；X-WAM/DP 相关 tmux 全退出（h100 `xwam_srv0/1`、`xwam_broker`；wls `xwam_srv0`、`dp_ladder`、`dp_latency`；t108 `xwam_k*`、`xwam_smoke_r23`；本地 `xwam_ladder`）。巡检 cron 已删。
- 环境与模型件留在原位（未接到删除指示）：h100 `/data/xwam`（ckpt 109 GB + Wan2.2 32 GB）；wls `/home/weiland/x-wam`（代码+venv）、`/data/xwam`、`/home/weiland/dp`（代码+micromamba env）、`/data/dp`（ckpt 20 GB + lowdim 数据 + results）；t108 `/scratch/zixuans8/{xwam,dp}`。
- 本地产物：`exp/xwam_nfe/`（ops + data/results_k* + aggregate_xwam.json）、`exp/dp_nfe/`（eval_dp_steps.py、aggregate_dp.py、ops、data/results + aggregate_dp.json）。**未 commit 未 push。**

## 6. X-WAM × RoboTwin 2.0（demo_randomized）减步阶梯（owner 09:0x 排队："受控实验之后做"，先做这个）
- **为什么**：owner 问"少跑无损是不是 benchmark 太简单"。已有折损证据只在 RoboCasa365（GR00T k=1 −11 pp、π0.5 −17 pp）；LIBERO / RoboCasa-2024 的示范都是单操作者或 MimicGen 合成、条件分布近单峰，1 步 = 条件均值自然无损。RoboTwin 2.0 randomized（50 任务、双臂、背景/杂物/光照/桌高随机）是 X-WAM 官方第二个 benchmark（Clean 89.8% / Randomized 90.7%），ckpt `robotwin_sft` 已在 h100/wls。**诚实标注**：RoboTwin 示范由专家程序 + CuRobo 规划器生成，不是多操作者人类数据，其"多峰"只有规划路径随机；真正的多峰对照仍是 §7 受控实验（PushT / robomimic MH / Franka Kitchen，x₀ 头 vs ε 头）。
- **评测协议（X-WAM `evaluation/robotwin_client.py`）**：每任务一个 client，`--task_config demo_randomized --instruction_type unseen --seed 0`（st_seed=100000，各 k 同 seed 序列），每个 seed 先跑一遍脚本专家（`play_once`，CuRobo）确认可解再评策略；策略每次查询 32 步 ee 动作（mplib IK），观测 = head + 左右腕 3 路 RGB（D435 320×240）+ 双臂 ee 位姿；步数上限 `_eval_step_limit.yml` 400–1700；结果 `<save_root>/<task>/_result.txt`（末行=成功率）+ 每集 mp4。
- **client 只能放 timan107**：t108 `cuInit` 仍 101（CuRobo 必需 CUDA——`load_robot` 无条件建 `CuroboPlanner`，专家回放也要它）；h100 无 RT 核且不跑模拟器；wls 按 owner 规则不跑模拟器。t107 = 8× GTX 1080 8 GB（Pascal，驱动 535，Vulkan ICD 在）。**RoboTwin 渲染是光线追踪**（`_base_task.setup_scene`：shader `rt`、32 spp、path depth 8、OIDN 去噪）——Pascal 上实测 SAPIEN 3.0.0b1 的 RT 能跑（驱动模拟），320×240 32 spp **34 ms/帧**（128 spp 118 ms）；但 OIDN 的 CUDA 设备要 Volta+（`OIDN Error: unsupported device type: CUDA`，帧不去噪），改用驱动自带 **OptiX 去噪**（`libnvoptix.so.1` 在，37 ms/帧）→ 记为一处渲染偏差（两者都是 AI 去噪器）。SAPIEN 按 `CUDA_VISIBLE_DEVICES` 选 Vulkan 设备（实测 GPU5 占 673 MiB）。
- **摆放**：t107 `/scratch` 只剩 6.5 GB（其他线占 97%），全部放 NFS home `/home/zixuans8/robotwin`（10 T 盘剩 175 GB）：`bin/micromamba`、`mamba/envs/rt`（py3.10 + `nvidia/label/cuda-12.1.1 cuda-toolkit` 供 nvcc；torch 2.4.1+cu121，`TORCH_CUDA_ARCH_LIST=6.1`）、`X-WAM/`（含 `third_party/RoboTwin` 子模块 c3ddfa8b + `envs/curobo` v0.7.8 源码编译 + assets 14 GB zip）。脚本 `exp/xwam_nfe/ops/setup_robotwin_t107.sh`（t107 tmux `rt_setup`，日志 `/tmp/robotwin/setup.log`，08:58 起）。
- **阶梯脚本**：server `serve_xwam.sh` 加 `XWAM_CKPT=robotwin_sft`（已推 h100/wls）；client `run_client_robotwin.sh <task> <n> <broker> <port> <gpu> <tag> <out>`（tmux `rtw_k<k>_<task>`）；控制器 `ladder_robotwin_local.sh <ks> [n=20] [video=50] [pool=16]`（h100 2 server + wls 1、broker 随重启；t107 每 GPU 2 client；`agg_robotwin.py` → `results_k<k>/summary.json` 拉回 `exp/xwam_nfe/data/robotwin_k<k>/`）。计划 k = 10（官方默认，复现 90.7%）/ 3 / 2 / 1，每任务 20 集起（冒烟测出每集时长后再定 50）。
- **09:5x timan107 磁盘整顿（owner：「你清理这些」）**：根盘（`/scratch` = `/`，212 GB）原剩 6.2 GB。删了四项（`rm -r` 不带 -f，删前逐项数清单、确认无进程占用）：`openpi/exp/ablation_study/data/distill_raw` 12 GB（与 wls `/archive/openpi/ablation_study/executor_substitution/distill_raw` 1004 文件/12,556,220,211 字节完全一致）、同目录 `checkpoints` 3.9 GB（ACT 中间步 000500/010000，archive 存的是终版 020000）、`cosmos/` 23 GB（Cosmos 线已停，RC24 结果已在本地）、`lerobot_venv/` 6.2 GB（执行体替换 ablation 已 ship）。根盘现剩 **51 GB**（75%），`/scratch/zixuans8` 101 → 57 GB。保留：Isaac-GR00T 33 GB（RoboCasa365 模拟器件）、openpi/.venv 8.1 GB + libero_sim 5.1 GB（LIBERO client）。待 owner 裁：`rl_router/m6` 2.3 GB；NFS 上 `t107_scratch_offload__NFS_SLOW__via_symlink` 6.8 GB。
- **RoboTwin 安装改到本地 RAID `/srv/local/zixuans8/robotwin`**（owner：不准装 NFS；`/srv/local` 是 7.3 TB ext4 RAID5，剩 108 GB）；NFS 上的半成品 `/home/zixuans8/robotwin`（40 GB）已 `rm -r`。09:44 从头重装，本地盘上 CUDA 工具链 2 min 装完（NFS 那次 4 min+），curobo 编译中。
- **10:1x t107 冒烟失败 → 这条线的 client 改到 weilandserver**（owner 10:2x 裁定）：RoboTwin 环境在 t107 装通（渲染 OK、import 全过），但专家回放第一步 CuRobo 的 LBFGS CUDA kernel 在 GTX 1080 上 `CUDA error: invalid argument`（client 吞成 `error occurs !` 无限重试）；CuRobo 源码里共享内存扩容明写 "only available from volta+"，Pascal 不在支持面。t108：`cuInit` 101 自 9-16 23:05 前（dmesg 环形缓冲起点）持续至今，根因是第 4 张 A5000 `0000:c1:00.0` `RmInitAdapter failed (0x22:0x56:762)`，owner 试 sudo 发现不在 sudoers；已给运维一句话（不重启：停 persistenced → `unbind` → PCI `remove` → 重启 persistenced，`nvidia-smi -L` 应见 3 张）。**t107 上的 RoboTwin 安装（`/srv/local/zixuans8/robotwin` 36 GB + `/tmp/robotwin`）已删**，根盘 51 GB、/srv/local 128 GB 空闲。
- **wls 安装（10:29 起，tmux `rt_setup`，`exp/xwam_nfe/ops/setup_robotwin_wls.sh`，日志 `/tmp/robotwin/setup.log`）**，按摆放纪律：代码+env 在 SSD `/home/weiland/robotwin`（micromamba env `rt`：py3.10 + `cuda-12.4.1 cuda-toolkit`（系统 nvcc 是 13.2，与 torch 不配；gcc 13.3 要 CUDA ≥12.4）+ torch 2.5.1+cu124 + sapien/mplib/curobo(sm_89)），资产/缓存/结果在 `/data/robotwin`（`assets/` 14 GB 软链进子模块 `third_party/RoboTwin/assets`，`cache/{pip,hf}`，`results_k<k>/`）。4090 是 Ada：硬件 RT + OIDN CUDA 原生，**不需要** t107 那个 OptiX 替换，渲染与官方一致。wls 不再跑 X-WAM server（h100 两个够）；控制器 `ladder_robotwin_local.sh` 改为 client 在 wls、池 8（48 GB 显存）。
- **11:0x–11:51 wls 冒烟通过 + 阶梯起跑 + 一小时自找的坑**：冒烟 adjust_bottle 2/2（每集 ~1 min，h100 server 0.39 s/查询，k=10）。阶梯 11:02 起（本地 tmux `rtw_ladder`，k=10,3,2,1 × 50 任务 × 20 集，池 8），但每波只有第一个 client 活、其余起手就 `SapienRenderer()` 段错误（exit 139）。先后怀疑并处理了三处（都留着、无害）：Vulkan ICD 钉到 `nvidia_icd.json`（wls 装了 9 个 ICD）、每 client 独占 NVIDIA 着色器磁盘缓存（`__GL_SHADER_DISK_CACHE_PATH`，共享 `~/.cache/nvidia/GLCache` 确实被并发写）、flock 把启动串行成 25 s 一个。真正根因靠 launcher 里加的 `env` 快照对比出来：**控制器给 client 传的 GPU 号还是 t107 时代的 `(idx-1) % 8`**，单卡 wls 上 `CUDA_VISIBLE_DEVICES=7` → CUDA 无设备 → SAPIEN CUDA 互操作初始化崩；每波首个（idx=1）和 handover_block（idx=9）恰好取 0 所以活。改为固定 0 后 11:51 resume：8 client 全活、0 段错误，显存 38.5 GB（~4.8 GB/client，池 8 已是 48 GB 上限）、GPU 100%、load 7/88。教训：换 client 机器时把 launcher/控制器里所有机器相关参数（GPU 数、路径）逐项过一遍，别只改路径。
- **12:2x–13:2x 提速三件 + h100 加 client（owner：「1 2 都做」「4 可以对所有进程起效吗，我们可以重启进程」）**：
  ① `XWAM_NO_VIDEO=1`（`exp/xwam_nfe/ops/patch_deploy_no_video.py` 打在 `evaluation/X-WAM/deploy_policy.py`）：X-WAM client 每次查询后每 4 步再渲染 3 路相机拼 mp4，8 次渲染只为视频；跳过后策略输入不变（每查询仍 1 次 `get_obs`），每集 mp4 不再写。② `PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True`。③ **CUDA MPS**（wls/h100 各起 `nvidia-cuda-mps-control -d`，普通用户即可）：新进程自动挂 MPS，实测 client 显存 6.8–7.7 GB → **5.0–5.5 GB**；wls 池 7 → 8。只对新进程生效，所以把进度少/卡死的 3 个 client 杀掉重投，其余跑完当前任务自然换新。④ 控制器加**卡死检测**（日志 25 min 无更新即杀掉重投；handover_mic 曾在 "Policy Name" 后停 45 min 零集）。
  h100 client 环境：`setup_robotwin_h100.sh`（全在 `/data/xwam/{robotwin,robotwin_data}`，根盘剩 7 GB；micromamba + CUDA 12.4 + torch 2.5.1 cu124，sm_90），SAPIEN 在 H100 上 `rayTrace: 1`，32 spp 320×240 **9 ms/帧**。坑：CuRobo 的 `lbfgs_step` 融合 kernel 在 sm_90 上 `illegal instruction`（与 MPS 无关；4090 sm_89 正常，1080 sm_61 是 `invalid argument`）→ `patch_curobo_lbfgs_h100.py` 把 `LBFGSOpt.use_cuda_kernel` 置 False 走 CuRobo 自带 torch 路径（同一算法，浮点顺序略异；记为 h100 侧规划器的一处偏差）。冒烟 place_shoe 1/1。
  server 账：7 个 client 合计仅 **0.3 查询/s**（每 client ~23 s 一次；时间在仿真/IK/专家回放），单 server（0.39 s/次）能扛 2.5 q/s → h100 从 k=1 起改 **1 server + 7 client**（控制器 `rtw_nsrv`=1，池随 server 数自动 7/3）；k=10 期间保留 2 server（中途杀 srv1 有约 6% 概率让在途请求丢失、client 永久挂）+ 3 client。控制器 v3 双机版（`ladder_robotwin_local.sh`：池文件 `rtw_pool_wls`/`rtw_pool_h100` 每轮重读、任务派给空位最多的机器、两机 `_result.txt` 合并聚合）。
- **13:2x MPS 故障级联 → MPS 弃用**：13:00 起 wls 上挂在 MPS 的 client 于 13:2x 同时消失，此后每个新 client 都在 `Sapien_TEST` 建渲染器时失败（`Render Error`，exit 0），wls 卡空转 20 min 才被发现（GPU 2 GB/0%）——MPS server 进入故障态后同 server 所有 client 一起死、新进程建不了上下文；h100 上 MPS 下 3 个 client 反而占 8.9/7.7/5.7 GB，显存 79.6/81.6 GB 贴 OOM。owner 授权重启进程，两台 `echo quit | nvidia-cuda-mps-control`，client 全部重投，控制器 13:44 resume（wls 池 7、h100 池 2；k=1 起 h100 单 server + 6）。5 min 后 wls 7 client 全活、0 Render Error/段错误。教训：多进程仿真 client 不要用 MPS（一个进程的致命错误会连坐）。
- **14:4x–16:2x timan107 能不能跑（owner 要求研究）**：重装到 `/srv/local/zixuans8/robotwin`（15:13 完）。CuRobo 在 sm_61 上的 `invalid argument` 就是 `lbfgs_step` 融合 kernel（源码 `#define VOLTA_PLUS true` 写死，但其它 kernel 只用 `__shfl_*_sync`/`atomicAdd` 等 sm_30 指令，无 Volta+ 独有指令）→ 同 h100 的补丁（`use_cuda_kernel=False` 走 torch 版 L-BFGS）后过了 kernel 关；接着 8 GB 显存 OOM（RoboTwin 每臂建 2 个 MotionGen 并做 CUDA graph 预热，4 个规划器的 graph 缓冲把 7.9 GB 吃满）→ `patch_curobo_nograph_t107.py` 给两处 `MotionGenConfig` 加 `use_cuda_graph=False`（纯执行优化，规划结果不变）后专家回放 `PLAY_ONCE_OK`，显存 ~4 GB。**结论：不是架构绑死，两处补丁就能跑；但太慢**：冒烟 adjust_bottle 首集含 7 min 初始化 21 min，第二集稳态 **5 仿真步/min**（wls ~60 步/min，同代 Broadwell Xeon、单线程跑满 1 核、GPU 2%，慢 10× 的原因未定位），成功集 ~25 min、失败集 ~80 min → 8 卡合计 ≈ +0.3 集/min（+15%）且会拖长每档尾巴（一个 t107 client 能占着一个任务几小时）。**不接入**；安装保留在 `/srv/local`（36 GB）待 owner 裁。
- **16:07 wls 也关 CuRobo CUDA graph**（`patch_planner_nograph.py`，新起 client 生效）：目的是把每 client 显存从 5–7 GB 压到 ~4 GB，让 48 GB 卡放 11–12 个 client（CPU 88 核只用了 7 个，单 client 是单线程 CPU 绑定、GPU 功耗仅 120 W）。池先 7 → 9，观察新 client 稳态显存再提。
- **16:2x–16:5x 事故串 + 修正**：① wls 关 CUDA graph 后 client 稳态显存只降到 5.0–5.7 GB（1080 上 3.7–4 GB 是因为场景更小/无 RT 缓冲），9 个就 48.3/49 GB 顶满 → 池回 **8**，"塞 11 个"的设想不成立。② 16:26 wls 负载 16 时一次 tether 查询超时返回空，控制器把 wls 当成 0 完成/0 运行，**把 8 个已完成任务重投**（VRAM 顶满、两个 client 撞 OOM 后 CUDA 上下文粘滞、每 seed 秒失败空转 12k 次，一个 client 卡死）；修：控制器主机查询必须回 `HOSTOK` 否则本轮跳过；回收器加 `error occurs` >300 次即杀（ERRLOOP）；重复 client 手动清、空转/卡死的杀掉重投。③ py-spy 采样（7 min，click_bell）没进 rollout，只看到 setup：`load_robot`（建 CuRobo 规划器）39%、建桌/墙 6%、原生帧 48%——rollout 阶段的分布待下次采（要在正常显存状态下采）。tether 全车队更新期间控制器只跳过、无误动。

## 7. 模拟效率研究（owner 16:5x「发 agent 组去研究模拟优化问题」；4 个只读 agent，17:35 出齐）
- **最重要的发现（agent 1/4，实测）**：X-WAM 的 `ee` 模式每个动作步调 **CuRobo `plan_single` 两次**（不是 mplib），每次规划 = 上万个小 kernel；我 16:07 为省显存关掉的 `use_cuda_graph` 让规划 launch-bound，16:07 后起的 client **10 步/min vs 之前 60–100 步/min（慢 6–8×）**；t107 慢 10× 也是它（Pascal + 无 graph + torch LBFGS）。17:35 已回退 planner.py、杀掉 8 个慢 client 重投（丢 25 集）。每动作步 ~0.75 s 里 CuRobo 30–50%、物理子步（100–250 个 250 Hz 步）30–50%、RT 渲染 + server RTT 5–10%。
- **数据有效性问题（agent 3）**：SAPIEN 的 OIDN 去噪器显存不足时**只打日志、静默把未去噪帧交给策略**（`denoiser_oidn.cpp:168`）。审计 k=10 已完成任务的产生日志：**11 个任务含 `OIDN Error: out of memory`**（grab_roller 197 条、handover_mic 152、place_bread_skillet 88、place_cans_plasticbox 77、place_a2b_right 66、pick_dual_bottles 46、place_bread_basket 45、place_burger_fries 36、pick_diverse_bottles 20、place_a2b_left 20、move_playingcard_away 19；多为 16:2x 显存顶满时段）+ 5 个产生日志被 16:26 误重投覆盖无法核验（beat_block_hammer、blocks_ranking_rgb/size、click_alarmclock、click_bell）。**16 个结果全部隔离到 `/data/robotwin/results_k10_tainted/`（附日志）重跑**；剩 8 个干净 + h100 1 个。控制器回收器加 `OIDN Error` 即杀重投（RENDER_OOM）；`clear_cache_freq` 5→1（结果不变，省 1–2 GB/client）；池 7（graph client 6–7 GB，须留 ≥1.5 GB 余量）。
- 其它结论：单进程单核是因为 SAPIEN 的 `step`/`take_picture` 不释放 GIL、PhysX 无工作线程（`cpu_workers` 不可改：非确定性）；线程数 100–229 全是空闲池，无超订；GPU PhysX/批量 RT 不可用且改数值；多环境单进程只省显存不省 CPU；专家检查缓存只省 10–15%（agent 2 补丁已写，`RTW_EXPERT_CACHE` 读写、含 k=10 verdict 文件方案，未上）；两处严格结果不变的小优化：每物理步重复 8 次 `_entity_qf`（省 5–8%）、非 crazy-light 集跳过每步 `_update_render`（省 5–10%）。协议本身的非确定性：CuRobo IK Halton 种子跨进程有状态、LBFGS history>12 非确定、指令用未播种的 Python `random`；`XWAM_NO_VIDEO` 在 2% crazy-light 集里改光照 RNG。**吞吐模型**：graph 开 + 8 client ≈ 185 集/h（4090）；再把显存压到 ≤2.4 GB 放 20 个 + MPS 才到 ~460（MPS 已证明会级联，不做）。
- **18:0x owner 裁定：只跑 k=10 和 k=1（各 20 集/任务），k=3/2 不跑**。控制器以 `10,1` 重启（resume）。wls 池 6、h100 池 1（k=1 起 h100 单 server + 6 client 的规则改为单 server + 4，留去噪器余量）。
- **23:09 RoboTwin k=10（官方默认 10 步）完成**：macro **0.9080**（50 任务 × 20 集，wls 43 任务 + h100 7 任务；16 个 OIDN 污染结果已重跑替换），**官方 randomized 90.7% 精确复现**。21 个任务 20/20；最低 hanging_mug 0.60、turn_switch 0.60、place_mouse_pad 0.70、put_object_cabinet 0.70、place_dual_shoes 0.75、press_stapler 0.75。数据 `exp/xwam_nfe/data/robotwin_k10/{summary.json,merged/,weilandserver/,h100/}`（每任务 `_result.txt`），mp4 因 NO_VIDEO 未存。k=10 有效吞吐（17:36 修好 graph 后）≈ 8 任务/h ≈ 160 集/h（wls 6 + h100 1–2）。**k=1 23:09 起**：h100 1 server + 4 client、wls 6 client，共 10 路；预计 05:30 前后出结果。
- **2026-09-18 10:21 RoboTwin k=1 完成 → X-WAM × RoboTwin 2.0 阶梯收官**（owner 裁定只跑 k=10/k=1）：k=1 macro **0.8640**（864/1000）vs k=10 **0.9080**（908/1000），**−4.4 pp**（50 任务 × 20 集，同 seed 序列；macro 的 Wilson 95% CI 半宽约 ±1.9 pp，配对差显著）。折损集中在 7 个任务（open_microwave 0.95→0.25、hanging_mug 0.60→0.25、place_bread_basket 1.00→0.65、move_stapler_pad 0.80→0.55、blocks_ranking_size 0.90→0.70、handover_mic 0.95→0.75、place_dual_shoes 0.75→0.55），2 个任务 +10 pp（put_object_cabinet、stamp_seal，噪声级），其余 41 个持平。**这是 X-WAM 第一次在 1 步出现明确折损**（RoboCasa-2024 上 0.793 vs 0.792 无损）；掉的是开门/挂杯/放篮/递话筒这类精细定位或双臂协同任务。数据 `exp/xwam_nfe/data/robotwin_k1/{summary.json,merged/,weilandserver/,h100/}`。k=1 尾巴：最后两个任务（put_bottles_dustbin 从 h100 慢 client 改投 wls、shake_bottle 因 tmux `has-session` 前缀匹配到 `shake_bottle_horizontally` 而三次"已在跑"被跳过——launcher/控制器已改精确匹配 `-t =name`）。h100 server/broker、两台 client 全停，GPU 归零；结果未 commit。
- 与 RoboCasa-2024 的对照口径：同一 X-WAM 架构、同一 `--action_denoise_steps`；RoboTwin randomized 的示范由专家程序 + CuRobo 生成（非人类多操作者），折损应解释为"更难/更精细的任务分布"而非"人类多峰"——多峰的正面证据留给 x₀ 头受控实验（`logs/x0_multimodal_plan.log.md`）。

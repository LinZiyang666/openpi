# Step 1: 拆分推理管线 — 完整记录

> 状态：**已完成并验证**（server 以 `--cache` 启动运行正常）
> commit: `a6c9f43` on branch `Ziyang`
> 日期：2026-03-29

---

# 一、规划阶段

> 目标：为缓存系统的 CP1/CP2/CP3 拦截点提供类型化的公共接口，同时**零侵入**现有推理路径和计时系统

## 1. 现状分析（代码阅读结论）

### 1.1 已有的私有 Stage 拆分

`pi0_pytorch.py` 中已经存在三个私有方法：

```python
_stage1_token_prep(observation)
    → (state, prefix_embs, prefix_pad_masks, prefix_att_2d_masks_4d, prefix_position_ids)

_stage2_llm_backbone(prefix_embs, prefix_pad_masks, prefix_att_2d_masks_4d, prefix_position_ids)
    → past_key_values

_stage3_action_expert(state, prefix_pad_masks, past_key_values, noise, num_steps=10)
    → x_t  (action_chunk)
```

`sample_actions()` 已经通过这三个私有方法串联执行，不需要重写其逻辑。

`policy.py` 已通过 `hasattr(model, "_stage1_token_prep")` 检测并使用这三个私有方法做**按阶段计时**，将结果写入 `stage_timing` 字段。

### 1.2 现有计时系统

`policy.py` 中的现有计时系统：
- 使用 `time.monotonic()` 计时（CPU 时间）
- 在 CUDA 推理后调用 `torch.cuda.synchronize()` 对齐 GPU 操作
- 输出 `stage_timing = {token_prep_ms, llm_backbone_ms, action_expert_ms, total_ms}`

**Step 1 不得修改此系统**。缓存系统的精细计时将在 Step 2（`SystemTimer`）中实现。

### 1.3 缺失的内容（Step 1 需要补充）

| 缺失内容 | 用途 |
|---------|------|
| `Stage1Output` / `Stage2Output` / `Stage3Output` 数据类 | 阶段间传递数据，供 `InferenceInterceptor` 使用 |
| `run_stage1()` / `run_stage2()` / `run_stage3()` 公共方法 | 缓存系统的拦截点 |
| `run_stage3()` 的 `return_intermediates` 参数 | 保存流匹配中间状态 x_t，用于 Warm Start |
| `run_stage3_from(start_x, start_t)` | Warm Start 从中间状态继续去噪 |

---

## 2. Pi0 与 Pi0.5 的关键区别

### Stage 1（视觉编码 + Prefix 嵌入）

| 方面 | Pi0 (`pi05=False`) | Pi0.5 (`pi05=True`) |
|------|-------------------|---------------------|
| 状态输入 | `state` 是连续向量，在 Stage 3 的 Suffix 中处理 | `state` 已被变换管线离散化为文本 token，编码进 `lang_tokens` |
| Prefix 内容 | 图像 token + 语言 token | 图像 token + 语言 token（其中已含离散化状态） |
| `Stage1Output.state` | 原始连续向量，用于 Stage 3 的 `state_proj` | 原始连续向量，**不**进入模型，仅作为缓存键 |

> 关键：两种模型 `_stage1_token_prep` 逻辑相同，差异在下游（`embed_suffix` 分支）。

### Stage 2（LLM Backbone / KV Cache 填充）

- **两种模型逻辑相同**：均调用 PaliGemma 做前向传播并返回 `past_key_values`
- PyTorch 实现中 Pi0.5 **没有**自回归子任务生成（JAX 路径已禁用）
- `past_key_values` 是 HuggingFace `DynamicCache` 对象，**必须原样传递，不能 clone/reshape**

### Stage 3（Action Expert / 流匹配去噪）

| 方面 | Pi0 | Pi0.5 |
|------|-----|-------|
| Suffix token 数量 | 1（state）+ 50（action）= 51 | 50（action only） |
| 时间步注入 | 时间 embedding 与 action 拼接后经 MLP | 时间 embedding 经 `time_mlp` 后作为 adaRMSNorm 条件 |
| `adarms_cond` | `None` | 时间 MLP 输出的向量 |

> 去噪循环本身（Euler 步进）对两种模型相同，差异全部在 `denoise_step → embed_suffix` 内部，不影响 Stage 3 的接口设计。

---

## 3. 编码方案

### 3.1 文件改动范围

| 文件 | 改动内容 | 侵入程度 |
|------|---------|---------|
| `src/openpi/models_pytorch/pi0_pytorch.py` | 添加 3 个 dataclass + 5 个方法 | **仅添加**，不修改任何已有代码 |
| `src/openpi/cache/__init__.py` | 新建模块标记 | 新建 |
| `src/openpi/cache/interceptor.py` | 新建 `InferenceInterceptor` | 新建 |
| `scripts/serve_policy.py` | 添加 `--cache` 参数 | +13 行 |
| `src/openpi/policies/policy.py` | **不改动** | 零 |
| `src/openpi/models_pytorch/gemma_pytorch.py` | **不改动** | 零 |

### 3.2 张量形状总结（B=1, gemma_2b）

**Stage 1 输出**

| 字段 | 形状 | dtype |
|------|------|-------|
| `state` | `[B, 32]` | float32/bfloat16 |
| `prefix_embs` | `[B, ~968, 2048]` | bfloat16 |
| `prefix_pad_masks` | `[B, ~968]` | bool |
| `prefix_att_2d_masks_4d` | `[B, 1, ~968, ~968]` | float32 |
| `prefix_position_ids` | `[B, ~968]` | int64 |

> Pi0.5 prefix_len ≈ 968（768 视觉 + 200 语言）；Pi0 ≈ 816（768 + 48）

**Stage 2 输出**：`past_key_values`（DynamicCache，18 层，每层 k/v 各 `[B, num_kv_heads, prefix_len, head_dim]`）

**Stage 3 输出**：`action_chunk` `[B, 50, 32]` float32；可选 `intermediates[t]` 同形

### 3.3 关键注意事项

- `past_key_values` 禁止 clone/deepcopy，全程引用传递
- `run_stage*` 方法不加 `@torch.no_grad()`，由调用方控制上下文
- `torch.compile` 只作用于 `sample_actions()`，新方法不受影响
- `InferenceInterceptor` 实现 `BasePolicy` 接口，对 server 透明替换

---

# 二、实际代码修改（git diff 摘要）

## commit 信息

```
a6c9f43  Step 1: split inference pipeline into staged public API
5 files changed, 947 insertions(+)
```

## 文件 1：`src/openpi/models_pytorch/pi0_pytorch.py`（+256 行）

**新增 import**：
```python
from dataclasses import dataclass
from typing import Any, Optional
```

**新增 3 个 dataclass**（文件顶部，`PI0Pytorch` 类之前）：

- `Stage1Output`：包含 `state`, `prefix_embs`, `prefix_pad_masks`, `prefix_att_2d_masks_4d`, `prefix_position_ids`
- `Stage2Output`：包含 `stage1: Stage1Output`, `past_key_values: Any`
- `Stage3Output`：包含 `action_chunk: Tensor`, `intermediates: Optional[dict[float, Tensor]]`

**新增 5 个方法**（`PI0Pytorch` 类内，`_stage3_action_expert` 与 `sample_actions` 之间）：

| 方法 | 行数 | 说明 |
|------|------|------|
| `run_stage1(observation)` | ~15 行 | 调用 `_stage1_token_prep`，返回 `Stage1Output` |
| `run_stage2(stage1)` | ~12 行 | 调用 `_stage2_llm_backbone`，返回 `Stage2Output` |
| `run_stage3(stage2, *, noise, num_steps, return_intermediates, save_timesteps)` | ~45 行 | `False` 时零开销委托 `_stage3_action_expert`；`True` 时调用 `_stage3_with_intermediates` |
| `run_stage3_from(stage2, start_x, start_t, *, num_steps)` | ~35 行 | Warm Start：从 `start_t` 继续 Euler 步进至 0 |
| `_stage3_with_intermediates(state, prefix_pad_masks, past_key_values, noise, num_steps, save_timesteps)` | ~30 行 | 等价于 `_stage3_action_expert`，额外在指定时间步 `.clone()` 保存 `x_t` |

**所有已有代码（`_stage1_token_prep`、`_stage2_llm_backbone`、`_stage3_action_expert`、`sample_actions`、`denoise_step` 等）零修改。**

## 文件 2：`src/openpi/cache/__init__.py`（新建，2 行）

```python
# Cache system for Pi0/Pi0.5 inference pipeline.
# See docs/cache_system_architecture.md for the full design.
```

## 文件 3：`src/openpi/cache/interceptor.py`（新建，137 行）

实现 `InferenceInterceptor(BasePolicy)`：

- `__init__(policy)`：借用 `policy._model`、`_input_transform`、`_output_transform`、`_pytorch_device`（引用，不拷贝）
- `infer(obs, *, noise=None)`：
  1. 复用原 Policy 的输入变换管线
  2. `with torch.no_grad()`: `run_stage1` → `run_stage2` → `run_stage3`，每阶段后 `cuda.synchronize()` 计时
  3. 输出格式与 `Policy.infer()` staged 路径完全一致：`actions`, `state`, `policy_timing`, `stage_timing`
  4. `TODO(Step 4)` 注释标记 CP1/CP2 缓存检查的插入点
- `metadata` property：转发至原 Policy

## 文件 4：`scripts/serve_policy.py`（+13 行）

```python
# Args dataclass 新增字段
cache: bool = False

# main() 新增 wrapping 逻辑
if args.cache:
    from openpi.cache.interceptor import InferenceInterceptor
    logging.info("Cache mode enabled: routing inference through InferenceInterceptor.")
    policy = InferenceInterceptor(policy)
```

用法：
```bash
uv run scripts/serve_policy.py --cache --port 8001 policy:checkpoint \
    --policy.config=pi05_aloha_sim \
    --policy.dir=<checkpoint_dir>
```

> 注意：`--cache`、`--port` 等顶层参数必须放在子命令 `policy:checkpoint` **之前**（tyro 的参数解析规则）

---

# 三、验证结果

| 验证项 | 结果 |
|--------|------|
| AST 语法检查（所有新增文件） | ✅ 通过 |
| server 以 `--cache` 启动 | ✅ `INFO: Cache mode enabled` + `listening on 0.0.0.0:8001` |
| 原有计时系统（`stage_timing` 字段） | ✅ 由 interceptor 输出相同字段，server 汇总打印正常 |
| 外部接口一致性 | ✅ WebsocketPolicyServer 和客户端无需任何修改 |

---

# 四、遗留事项（后续 Step 处理）

- **Step 2**：实现 `SystemTimer`（CUDA event 精细计时），替换 interceptor 中的 `time.monotonic()`
- **Step 3**：实现 `CacheStorage`（FAISS CPU VectorStore）+ 数据结构（`CacheEntry`、`CacheResult`）
- **Step 4**：在 `interceptor.py` 的 `TODO(Step 4)` 处插入 `CacheOrchestrator.check(CP1/CP2/CP3)`
- **Step 7**：验证 `run_stage3_from()` warm start 精度与速度的 tradeoff
